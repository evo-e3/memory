#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import setup

setup(
    name='itabas-memory',
    packages=['itabas-memory'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)